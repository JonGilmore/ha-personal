---
cloud:
  google_actions:
    filter:
      include_entities:
        - scene.chill_on
        - group.all_lights
    entity_config:
      scene.chill_on:
        name: relax
  alexa:
    filter:
      include_entities:
        - scene.chill_on
        - group.all_lights
    entity_config:
      scene.chill_on:
        name: relax
        description: relax
